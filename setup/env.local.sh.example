#!/usr/bin/env bash
# env.local.sh.example
# Copy to env.local.sh and edit. Never committed.
# WSL2 machine: force rendering onto the discrete GPU and pin transport to loopback.

export MESA_D3D12_DEFAULT_ADAPTER_NAME=NVIDIA
export GZ_IP=127.0.0.1
export ROS_LOCALHOST_ONLY=1
